/*
 * /imports/client/components/app_preamble/app_preamble.js
 *
 * Display a small content in the page as the three-areas
 * 
 * - preamble: the HTML text displayed on the top
 * - image: the image URL displayed on the bottom-left
 * - template: the template rendered in the bottom-right
 */

import _ from 'lodash';
import { strict as assert } from 'node:assert';

import './app_preamble.html';
